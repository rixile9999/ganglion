# Phase 2 trained artifacts: regenerated from synth + bootstrap scripts.
# Reports + data + metrics are committed; weights and trainer state are not.
*/adapter/
*/trainer/
*/*/adapter/
*/*/trainer/

# Throwaway logs and smoke artifacts — runner logs are stderr captures,
# rerunnable from the same scripts. Kept out of history.
**/*.log
**/_smoke_*
**/_bootstrap_*.log
**/_patch_smoke/
